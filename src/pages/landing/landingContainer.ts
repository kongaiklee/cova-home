/** Shared container for landing sections so content aligns across Hero, Products, Handles, Features, Footer. */
export const LANDING_CONTAINER_CLASS =
  'mx-auto w-full max-w-7xl px-6 sm:px-16 lg:px-20';
